import {players} from '../data/players.js?v=hero-icons-4';import {t} from './i18n.js';
import {heroTeamMarkup} from './hero-team.js?v=hero-icons-4';
let state={q:'',sort:'activity',view:'list'};const fmt=n=>n==null?'—':new Intl.NumberFormat(document.documentElement.lang==='en'?'en-US':'ru-RU').format(n);
function team(p,type){if(type==='hero'&&p.heroTeam?.length)return `<div class="hero-team"><div class="team-label">${t('heroTeam')}</div>${heroTeamMarkup(p.heroTeam)}</div>`;const src=type==='hero'?null:p.titanImage;const cls=type==='hero'?'hero-team':'titan-team';if(!src)return `<div class="${cls}"><div class="team-label">${t(type==='hero'?'heroTeam':'titanTeam')}</div><div class="no-data">${t('noData')}</div></div>`;return `<div class="${cls}"><div class="team-label">${t(type==='hero'?'heroTeam':'titanTeam')}</div><img class="team-strip" src="${src}" alt="${p.nickname} ${t(type==='hero'?'heroTeam':'titanTeam')}"></div>`}
function render(){let arr=players.filter(p=>p.nickname.toLocaleLowerCase().includes(state.q.toLocaleLowerCase()));const sorts={activity:(a,b)=>b.activityTotal-a.activityTotal,total:(a,b)=>((b.heroPower||0)+(b.titanPower||0))-((a.heroPower||0)+(a.titanPower||0)),hero:(a,b)=>(b.heroPower||0)-(a.heroPower||0),titan:(a,b)=>(b.titanPower||0)-(a.titanPower||0),name:(a,b)=>a.nickname.localeCompare(b.nickname)};arr=[...arr].sort(sorts[state.sort]);const root=document.querySelector('[data-roster]');root.className=`roster ${state.view==='cards'?'cards':''}`;root.innerHTML=arr.map(p=>`<article class="player-card"><div class="rank">#${p.rank}</div><div><div class="nick">${p.nickname}</div><div class="meta">${t('activity')}: ${fmt(p.activityTotal)} · ${t('weekly')}</div></div>${team(p,'hero')}<div class="power"><span>${t('heroPower')}</span><strong>${fmt(p.heroPower)}</strong></div>${team(p,'titan')}<div class="power titan-power"><span>${t('titanPower')}</span><strong>${fmt(p.titanPower)}</strong></div><div class="activity-box"><div class="team-label">${t('totalPower')}</div><div class="activity-total">${fmt((p.heroPower||0)+(p.titanPower||0))}</div></div></article>`).join('');}
document.querySelector('[data-search]').addEventListener('input',e=>{state.q=e.target.value;render()});document.querySelector('[data-sort]').addEventListener('change',e=>{state.sort=e.target.value;render()});document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>{state.view=b.dataset.view;document.querySelectorAll('[data-view]').forEach(x=>x.classList.toggle('active',x===b));render()}));render();document.addEventListener('languagechange',render);
